サーバーの起動方法として
rails s
を実行する方法と
bin/rails server -b 0.0.0.0 -p 3000
のようにip addressをbindして起動する方法がある。

基本的にMVc(Model, View, Controll)モデルで開発を行う。
Modelはデータベースなどでデータを管理する。
ViewはHTMLでデータを表示したり利用者からの画面入力を受け付ける。
ControllはModelからデータを読みだして成形してViewに引き渡したり、Viewからデータを受け取って成形してModelに格納したりする。
